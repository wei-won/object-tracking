=================Contents====================
- DLT
  The original model (using SDAE) provided by Naiyan Wang, with pre-training and new video/dataset involved. For multi-object tracking.

- DLT_nopretraining
  The original model (using SDAE) provided by Naiyan Wang, with NO pre-training. For multi-object tracking.

- Single_DLT_nopretraining
  The original model (using SDAE) provided by Naiyan Wang, with NO pre-training. For single-object tracking.

- Data
  Stores the videos used.
