=================Contents====================
- Multi_CNN_Tracker
  The modified visual tracker (using Deep CNN as classifier), with pre-training. For multi-object tracking.

- Multi_CNN_Tracker_no_pretraining
  The modified visual tracker (using Deep CNN as classifier), with NO pre-training. For multi-object tracking.

- Single_CNN_Tracker_no_pretraining 
  The modified visual tracker (using Deep CNN as classifier), with NO pre-training. For single-object tracking.

- Data 
  Stores the videos used.